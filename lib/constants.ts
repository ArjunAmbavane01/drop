export const APP_NAME = "Drop";
export const ROOM_CODE_LENGTH = 8;
export const MAX_TEXT_LENGTH = 100_000;
export const SSE_POLL_INTERVAL_MS = 1_500;
export const SSE_KEEPALIVE_MS = 15_000;
/** Maximum number of files (flat, not folders) that can be picked in one upload batch */
export const MAX_UPLOAD_FILES = 50;
/** Maximum number of top-level items that can be bulk-selected at once */
export const MAX_BULK_SELECTION = 50;
